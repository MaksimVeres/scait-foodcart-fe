import { ErrorResponseModel } from './error-response-model';

describe('ErrorResponseModel', () => {
  it('should create an instance', () => {
    expect(new ErrorResponseModel()).toBeTruthy();
  });
});
