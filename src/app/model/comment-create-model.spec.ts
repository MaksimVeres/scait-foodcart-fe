import {CommentCreateModel} from './comment-create-model';

describe('CommentCreateModel', () => {
  it('should create an instance', () => {
    expect(new CommentCreateModel()).toBeTruthy();
  });
});
